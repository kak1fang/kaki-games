class Line{
    constructor(x1,y1,x2,y2){
        this.x1 = x;
        this.y1 = y;
        this.x2 = x;
        this.y2 = y;
    }

    draw(){
        Line(this.x1, this.y1, this.x2, this.y2);
    }
}