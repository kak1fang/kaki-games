class Mapcreater {

    constructor(graph) {
        this.graph = graph;
    }

    detectingClick(){
    const selectVertex  = getVertexAtPoint(x,y);
    const se

    if(!selectVertex){
        addVertex(graph,x,y);
    }

    if(selectVertex ===)


    }
}